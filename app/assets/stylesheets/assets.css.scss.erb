@font-face {
  font-family: TrajanPro;
  src: url(<%= asset_path('trajan-pro.otf') %>);
}

.pattern {
  background: url(<%= asset_path('header-pattern.png') %>) repeat scroll center center transparent;
}
