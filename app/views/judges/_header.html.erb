<header>
  <div class="pattern">
    <div class="container">
      <h1>
        <%= t '.open_search' %>
        <small>
          <%= translate_with_count Court.total,      :court %>,
          <%= translate_with_count Judge.total,      :judge %>,
          <%= translate_with_count Hearing.total,    :hearing %>,<br/>
          <%= translate_with_count Decree.total,     :decree %> a
          <%= translate_with_count Proceeding.total, :proceeding %>
        </small>
      </h1>
    </div>
  </div>
</header>

<%= t '.average_salary' %>