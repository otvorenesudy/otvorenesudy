Gmaps.<%= map_id %>.callback = function() {
  markers = Gmaps.<%= map_id %>.markers
  for (var i = 0; i < markers.length; i ++) {
    markers[i].infowindow.open(Gmaps.<%= map_id %>.map, markers[i].serviceObject);
  }
}
