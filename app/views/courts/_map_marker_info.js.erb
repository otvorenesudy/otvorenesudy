Gmaps.<%= id %>.callback = function() {
  markers = Gmaps.<%= id %>.markers
  for (var i = 0; i < markers.length; i ++) {
    markers[i].infowindow.open(Gmaps.<%= id %>.map, markers[i].serviceObject);
  }
}
