<header>
  <div class="pattern">
    <div class="container">
      <h1>
        <%= yield :title %>
        <small>
          <%= translate_with_count Court.count,      :court %>,
          <%= translate_with_count Judge.count,      :judge %>,
          <%= translate_with_count Hearing.count,    :hearing %>,
          <%= translate_with_count Decree.count,     :decree %> a
          <%= translate_with_count Proceeding.count, :proceeding %>
        </small>
      </h1>
    </div>
  </div>
</header>
