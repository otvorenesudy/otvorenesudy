<%= t '.headline_html' %>
<%= Judge.with_most_related_people.size == 1 ? t('.record_holds') : t('.record_hold') %><br/>
<%= Judge.with_most_related_people.pluck(:name).to_sentence %>

<small class="total">
  <%= t '.sub_headline_html', total_courts: number_with_delimiter(Court.total) %>
</small>