<% provide :title, t('.title') %>

<div class="container">
  <h1 class="display-4 m-t-3 m-b-1"><%= yield :title %></h1>
  <div class="row">
    <div class="col-lg-6">
      <%= render 'devise/shared/messages' %>
      <%= form_for resource, as: resource_name, url: session_path(resource_name), html: { class: nil } do |f| %>
        <div class="form-group">
          <%= f.label :email %>
          <%= f.email_field :email, autofocus: true, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :password %>
          <%= f.password_field :password, class: 'form-control' %>
        </div>
        <%= f.hidden_field :remember_me, value: 1 if devise_mapping.rememberable? %>
        <button type="submit" class="btn btn-lg btn-outline-primary"><%= t '.submit' %></button>
      <% end %>
      <%= render 'devise/shared/links' %>
    </div>
  </div>
</div>
