<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%
       # link_to image_tag(asset_path('logo-white.png'), alt: :logo), root_path, class: :brand
       %>

      <nav class="nav-collapse collapse">
        <ul class="nav pull-left">
          <%= link_to "Otvorené Súdy", root_path, class: 'capital brand' %>
          <%= li_tag :home, 'Súdy', courts_path %>
          <%= li_tag :group, 'Sudcovia', judges_path %>
          <%= li_tag :comments, 'Pojednávania', hearings_path %>
          <%= li_tag :legal, 'Rozhodnutia', decrees_path %>
        </ul>
        
        <ul class="nav pull-right">
          <%= li_tag :signal, 'Štatistiky', stats_path %>
        </ul>
      </nav>
    </div>
  </div>
</div>

<div class="search">
  <%= form_tag nil, method: :get, class: 'form-search' do |f| %>
    <div class="input-append">
      <%= search_field_tag :search, params[:search], class: 'search-query' %>
      <%= button_tag icon_tag(:search), name: nil, class: 'btn' %>
    </div>
  <% end %>
  
  <%= link_to 'Rozšírené vyhľadávanie', nil %>
</div>

<%= yield :header %>
